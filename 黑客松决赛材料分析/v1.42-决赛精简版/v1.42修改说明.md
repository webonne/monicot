# v1.42 改了什么

基于 2026-09-16 那份分析里的建议，能在上台前做完的都做了。结构、页数、节奏一律没动。

## 片子

| 页 | 改动 | 为什么 |
|---|---|---|
| P1、P2 | 副标题和目录改成口语 | 原文像产品说明书 |
| P3 | 去掉“助手达成目标的情况尚待实测”，收尾改成“定位这一步就超时了” | 这页的任务是把张力立起来，不是泄掉 |
| P4、P5 | 删脚注，收尾句改口语 | 脚注在说“这只是示意”，观众不需要知道 |
| P6 | 脚注只留两个案例的日期 | 来源要交代，判断不用交代 |
| P8 | 数字标签改成“只有这1条请求 / 4条请求”，说明文字不再以P95为主 | 原图里平均、P95、最大三个数一样，评委一眼能看出是单样本 |
| P9 | “原因尚未确认”改成“这是下一步要打通的” | 同一个事实，一个是结尾，一个是开头 |
| P11 | 脚注去掉“尚不代表原因确认或故障恢复”；删掉“和上一页录像是两回事”那句 | 这页本来就是讲方向的；两个案例的日期脚注里已经有 |
| P12 | 图注加一句“应用层和K8s已经在查，中间件、存储正在接”；脚注缩成“结构示意图，不是某次故障的真实拓扑” | 让评委知道图里哪些是真的；日志没关联上的事 P9 已经说过 |
| P13 | 表头去掉“研发当前方案”，收尾加“叠加不替代” | 评委席可能就有研发负责人 |
| P14 | 图下说明改成“已走到第几步” | 全用将来时会显得什么都没做 |
| P15 | 去掉未连接的侧边栏，入口从三个改两个 | 没接上的东西不放提效点里 |
| P16 | 删掉“耗时比较”整行；后又改成掐表位（见下一节） | 两格都写“待实测”，等于把“没测”投在大屏上 |
| P17、P18 | 副标题和三栏文字改口语，P18删脚注 | 收尾页要干净 |
| 备用页 | 脚注和FAQ改口语 | 统一口气 |

保留的三条脚注：P8（单样本）、P12（示意图）、P17（收益待实测）。这三处不说清楚会被当成造假，其余的说多了是自我削弱。

## 第三章改名

赛道是 AI 提效，第三章从“创新点”改成“提效点”，目录、页眉、章节导航一起改。P12–P15 的标题也改成直接说省了什么：

| 页 | 原标题 | 新标题 |
|---|---|---|
| P12 | 观测云全链路分析：从用户请求到存储 | 全链路分析：分散的数据一屏看全 |
| P13 | 程序日志不清晰时的排查依据 | 日志没写清，也能缩小排查范围 |
| P14 | 排障SOP知识飞轮 | SOP知识飞轮：查过一次，下次照着用 |
| P15 | 将成熟恢复步骤交给技术支持 | SOP写到能照着做，技术支持才接得住 |

P16 的“这次记录里”改成“这次排障”，按钮改成“查看这次排障记录”。

## 第三章和第四章去重

两章原来都在讲“提效”，同一件事讲了三遍。改法：

- 第四章改名“能省什么”：第三章讲做法，第四章讲结果和怎么算账。
- P16 去掉“人工四步 vs 助手四步”的流程图（P4 画过了），换成三栏对照：人工翻日志 15 分钟（实测）、熟手用观测云 4 分钟（实测）、排障助手不到 3 分钟（实测，不挑人）。口径是“收到问题 → 拿到可用线索”，同一个卡慢案例。
- P17 删掉副标题（和 P15 标题是同一句话），只留三行分工表、两个目标和“最后看四个数”。
- P18 讲稿改成回顾口吻。
- P16 讲稿删掉“接口清单、前后时段对照、下一步建议”那句，P8、P9 刚讲过。
- 节奏：P16 35→25 秒，P17 45→35 秒，省下的 20 秒给 P8（40→50）和 P11（30→40），总数还是 575 秒。

## 讲稿

34 页讲者提示全部重写，`上台照着讲.md` 同步。原则：

- 每段像说话，不像念稿。
- “尚未”“不能相加”“口径”这类词，主讲里一章最多出现一次。
- 四条备答直接写在对应页的提示里：P7 的 csdp-tracing、P8 的单样本、P13 的与研发方案关系、P10 的 MateClaw。
- P16 留了一句方括号提示：如果上台前掐了表，就在这里报那一个数。

## 没做的

- 侧边栏没有重新截图，只是从主讲挪走。
- 文件夹没有拆成“上台用/过程记录”，怕动了 P31 备用页里的相对链接。README 里说明了哪几个文件是上台用的。
- 历次改稿记录（`*记录.json`、`*说明.md`）没有改写，它们是过程档案。

## 自检

- 34 页在 Chromium 里重新渲染过一遍，P8、P13、P15、P16 版式正常。
- 讲稿主讲约 2,000 字，575 秒，语速和 v1.41 相当。
- 录像、原图、二维码、SVG 文件都没动。
